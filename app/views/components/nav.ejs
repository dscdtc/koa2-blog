<div class="nav">
  <div class="ui grid">
    <div class="four wide column"></div>
    <div class="eight wide column">
      <% if(authorHead) { %>
        <a href="/posts"><h1><%= author.name %></h1></a>
        <p>----<%= author.bio %></p>
      <% } else { %>
        <a href="/posts"><h1><%= blog.title %></h1></a>
        <p><%= blog.description %></p>
      <% } %>
    </div>
  </div>
</div>
